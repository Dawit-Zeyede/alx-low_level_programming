#include "main.h"
/**
  * _isupper - check for lowercase characters
  * @c: the charater to be checked
  * Return: end of the program
  */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
}
