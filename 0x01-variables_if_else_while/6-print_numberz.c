#include <stdio.h>
/**
 * main - prints alphabet in lower case except q and e.
 *
 * Return: ends the program.
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
